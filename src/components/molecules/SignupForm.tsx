import React from "react";

export const SignUpForm: React.FC = () => {
	/**
	 * TODO: Create the Input form for Signup with the styled form components.
	 * User Error Handling will also be done here.
	 */
	return <React.Fragment />;
};
