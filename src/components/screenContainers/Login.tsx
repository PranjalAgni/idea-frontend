import React from "react";

export const LogInScreen: React.FC = () => {
    /**
     * TODO: Keep the App's Logo at top and add the login form with a remember me 
     * checkbox and login button. 
    */
	return (
		<React.Fragment />
	);
};
