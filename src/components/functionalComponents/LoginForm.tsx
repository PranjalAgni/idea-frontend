import React from "react";

export const LogInForm: React.FC = () => {
    /**
     * TODO: Create the Input form for Login with the styled form components. 
     * User Error Handling will also be done here.
    */
	return (
		<React.Fragment />
	);
};
