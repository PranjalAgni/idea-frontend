import React from "react";
import { View } from "react-native";
import { styles } from "./Home.style";
export const HomeScreen: React.FC = () => {
	/**
	 * TODO: This will be the landing screen with option to bring out login and signup form.
	 * If existing session is already present, control will move to User Dashboard.
	 */
	return <View style={styles.container}></View>;
};
