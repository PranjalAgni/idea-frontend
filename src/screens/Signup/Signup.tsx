import React from "react";

export const SignUpScreen: React.FC = () => {
	/**
	 * TODO: Keep the App's Logo at top and add the signup form with a remember me
	 * checkbox and signup button.
	 */
	return <React.Fragment />;
};
